- id: "Maven:io.micrometer:micrometer-registry-stackdriver:"
  curations:
    comment: |
      Package resides in its own directory within project's code repository.
    vcs:
      path: "implementations/micrometer-registry-stackdriver"
