- id: "Maven:org.apache.zookeeper:zookeeper-compatibility-tests:"
  curations:
    comment: |
      Package resides in its own directory within project's code repository.
    vcs:
      path: "zookeeper-compatibility-tests"
